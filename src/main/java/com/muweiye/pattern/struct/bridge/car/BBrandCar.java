package com.muweiye.pattern.struct.bridge.car;

import com.muweiye.pattern.struct.bridge.engine.Engine;

public class BBrandCar extends Car {

    public BBrandCar(Engine engine) {
        super(engine);
    }
}